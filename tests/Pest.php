<?php

declare(strict_types=1);

/*
|--------------------------------------------------------------------------
| Test Case
|--------------------------------------------------------------------------
|
| The suite covers the module logic that runs without a host application.
| Anything touching App\Models needs a real InvoiceShelf install, so those
| paths are exercised there rather than here.
|
*/
